import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import SurveyCoordinator from './SurveyCoordinator';

ReactDOM.render(
    <React.StrictMode>
        <SurveyCoordinator/>
    </React.StrictMode>,
    document.getElementById('root')
);
